<Window x:Class="Cinema.GUI.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cinema.GUI"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="CustomMessageBox" Height="200" Width="350">
    <Grid>
        <Border Background="White" CornerRadius="12" Padding="15" BorderBrush="#DDD" BorderThickness="1">

            <StackPanel>

                <!-- Title -->
                <TextBlock x:Name="txtTitle"
                       FontSize="18"
                       FontWeight="Bold"
                       Margin="0,0,0,10"/>

                <!-- Message -->
                <TextBlock x:Name="txtMessage"
                       TextWrapping="Wrap"
                       FontSize="14"
                       Margin="0,0,0,20"/>

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">

                    <Button Content="OK"
                        Width="80"
                        Margin="5"
                            x:Name="btnOk"
                        Click="Ok_Click"/>

                    <Button Content="Cancel"
                        Width="80"
                        Margin="5"
                            x:Name="btnCancel"
                        Click="Cancel_Click"/>
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</Window>
